<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="schweika.chatapplication">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Views.LoggedOff.LoggedOffActivity"
            android:label="Chat application" />
        <activity
            android:name=".Views.Login.LoginActivity"
            android:label="@string/title_activity_login"
            android:parentActivityName=".Views.LoggedOff.LoggedOffActivity" />
        <activity
            android:name=".Views.Register.RegisterActivity"
            android:label="@string/title_activity_register"
            android:parentActivityName=".Views.LoggedOff.LoggedOffActivity" />
        <activity android:name=".Views.Main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Views.RecyclerView.RecyclerViewActivity" />
        <activity
            android:name=".Views.LoggedIn.LoggedInActivity"
            android:theme="@style/AppThemeNoActionBar" />
    </application>
</manifest>